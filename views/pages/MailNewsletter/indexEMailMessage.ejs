
<div id="indexEMailMessage"  v-cloak>
    <div class="container">
        <div class="mx-auto " style="max-width: 500px">
            <a class="text-white " href="/welcome">❮</a>

        <h1>Email Service</h1>
        <p>Set up the your personalised email service!</p>

        <!--Link to the first component of the transactions -->
        <router-link to="/set-email-type" id="defi-button-mobile">Start</router-link>

        <!-- end desktop tabs -->
        <!-- this is where your component will render in your view -->
        <!-- Also there is the mapping of functions that get emmit to those in emailMessages parasails.page  -->
            <router-view class="mt-5"
                @set-type="setEmailType"
                @set-cycle="setEmailCycle"
                @set-tip="setTip"
                @add-email-service="createEmailService">
            </router-view>
        <!-- End component render -->
        </div>
    </div>
</div>

<%- /* Expose server-rendered data as window.SAILS_LOCALS :: */ exposeLocalsToBrowser() %>
